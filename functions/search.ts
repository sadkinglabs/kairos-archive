/** The /search route. Only link unfurlers are answered here; everything
 * else falls through to the static page. See src/functions/searchPreview. */
export { onRequest } from "../src/functions/searchPreview";
