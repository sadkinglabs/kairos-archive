---
/** A random deck generator and the walkthrough of how it is built. The
 * code the page runs is the code the page shows: one JavaScript file,
 * split at its section comments so each step sits beside its prose. */
import Base from "../../layouts/Base.astro";
import CodeExample from "../../components/CodeExample.astro";
import source from "../../fun/random-deck.js?raw";
import { ATLAS, COLLECTION, SPELLBOOK, SPLIT } from "../../fun/random-deck.js";
import { loadRegistry, orderedSets } from "../../data/registry";

const sections = source.split(/^(?=\/\/ ── )/m).map((chunk) => chunk.trim());
const step = (n: number) => sections.find((s) => s.startsWith(`// ── ${n}.`)) ?? "";
const sourceUrl = "https://github.com/sadkinglabs/kairos-archive/blob/main/src/fun/random-deck.js";
const elements = ["Air", "Earth", "Fire", "Water"];
/** The sets a deck may draw from: every real set, never the promo bucket. */
const { registry } = await loadRegistry();
const sets = orderedSets(registry.sets).filter((s) => s.set_code && s.set_code !== "999");
const lines = source.split("\n").length;
---
<Base title="Random deck" canonical="/fun/random-deck" description="Pick elements and a spell split, get a random Sorcery deck list drawn to the copy rules from the Kairos Archive query API. Plain JavaScript, on the page, step by step.">
  <p class="eyebrow"><a href="/fun">Fun Zone</a></p>
  <h1>Random deck</h1>
  <p class="intro">Pick the elements, the sets, how the spellbook splits and how many Toolbox, and deal: one avatar, a spellbook of {SPELLBOOK}, an atlas of {ATLAS}, and with any Toolbox a collection of {COLLECTION} Ordinary spells for it to cast, no card above what its rarity allows. Every part of the deck is one search query, the same syntax as the search box, answered by the <a href="/docs/query">query API</a>, and the list shows the query each part came from. Booster printings only, never promos. It is {lines} lines of plain JavaScript, all on this page.</p>

  <form id="deck-form" class="deck-form">
    <fieldset>
      <legend>Elements</legend>
      <div class="row">{elements.map((e) => <label><input type="checkbox" name="element" value={e} checked={e === "Fire" || e === "Water"} /> {e}</label>)}</div>
    </fieldset>
    <fieldset>
      <legend>Sets</legend>
      <div class="row">{sets.map((s) => <label><input type="checkbox" name="set" value={s.set_code} checked /> {s.set_name}</label>)}</div>
    </fieldset>
    <fieldset>
      <legend>Spellbook split</legend>
      <div class="row">{Object.entries(SPLIT).map(([type, n]) => <label>{type}s <input type="number" name={type} value={n} min="0" max={SPELLBOOK} /></label>)}</div>
    </fieldset>
    <fieldset>
      <legend>Toolbox</legend>
      <div class="row">{[0, 1, 2, 3].map((n) => <label><input type="radio" name="toolbox" value={n} checked={n === 0} /> {n}</label>)}</div>
    </fieldset>
    <button type="submit">Deal a deck</button>
  </form>
  <pre id="deck" class="rules deck" aria-live="polite">Your deck appears here.</pre>

  <h2>How it is built</h2>
  <p>JavaScript, the plain kind: no framework, no build step, no key. Nine parts, in the order they run; the code under each heading is read from the file this page is running.</p>

  <h3>0. The rules</h3>
  <p>Four copies of an Ordinary, three of an Exceptional, two of an Elite, one of a Unique. Sixty spells, thirty sites, ten in the collection. The default split by type, in the order the list shows it.</p>
  <CodeExample code={step(0)} lang="javascript" title="the rules" />

  <h3>1. One search query per part</h3>
  <p>The deck is nothing but search results. <code>t:minion (e:fire,water or e:none) s:001,006 is:booster</code> is every minion with Fire, Water or no element, printed in a booster of Alpha or Gothic. Paste it into the search box and you see the cards the deal draws from. The collection asks for <code>cat:spell rarity:ordinary</code>; Toolbox is its own name, <code>!"Toolbox"</code>, and when it is in, the artifacts leave it out.</p>
  <CodeExample code={step(1)} lang="javascript" title="the query" />

  <h3>2. Every card the query matches</h3>
  <p>The API answers two hundred cards a page and says whether there are more, so the loop asks until there are not. The answer is remembered: a second deal with the same choices asks nothing. The API allows sixty requests a minute from one address, and a deal is about ten.</p>
  <CodeExample code={step(2)} lang="javascript" title="fetching" />

  <h3>3. Only the chosen elements</h3>
  <p><code>e:fire,water</code> also admits a Fire and Air card, because it has Fire. One line keeps a card only when every element it has was chosen.</p>
  <CodeExample code={step(3)} lang="javascript" title="the filter" />

  <h3>4. Draw to the copy rules</h3>
  <p>Shuffle, then walk the pool taking a random number of copies of each card, never more than its rarity allows, until the part is full. A pool too small for its part gives a short part, which the list says, never a fifth copy.</p>
  <CodeExample code={step(4)} lang="javascript" title="the draw" />

  <h3>5. The sizes</h3>
  <p>The spellbook is sixty with Toolbox among them. The split is proportions: whatever Toolbox takes comes off first and the rest is shared out, rounded, with the odd card going to the largest type.</p>
  <CodeExample code={step(5)} lang="javascript" title="the sizes" />

  <h3>6. A deck</h3>
  <p>One query per part, one draw per part, in the order the list shows: the avatar, the spellbook by type, Toolbox, the sites, then the collection. The collection skips any card already in the deck, because a card's copies are counted across the whole deck.</p>
  <CodeExample code={step(6)} lang="javascript" title="the deal" />

  <h3>7. The list</h3>
  <p>Each part with its count and the query it came from, then its cards by name, then how many cards in all: 91, or 101 with a Toolbox and its collection.</p>
  <CodeExample code={step(7)} lang="javascript" title="the list" />

  <h3>8. The form</h3>
  <p>Read the boxes, deal, print. This is the only part that needs a browser.</p>
  <CodeExample code={step(8)} lang="javascript" title="the form" />

  <h2>Make it yours</h2>
  <p>Change the queries. <code>s:alpha</code> for one set, <code>cost&lt;=3</code> for a cheap deck, <code>-is:errata</code> to skip cards whose text has changed, <code>k:spellcaster</code> for a theme: anything the <a href="/syntax">search syntax</a> can say, the deck can be drawn from. Parts 0 to 7 run in Node as they are; send a <code>User-Agent</code> that names your tool, as the <a href="/usage">usage terms</a> ask.</p>

  <h2>All of it</h2>
  <p><a href={sourceUrl}>random-deck.js on GitHub</a>.</p>
  <CodeExample code={source} lang="javascript" title="random-deck.js" />
</Base>

<script>
  import "../../fun/random-deck.js";
</script>

<style>
  .deck-form { border: 1px solid var(--line); background: var(--card); border-radius: 8px; padding: .75rem 1rem; margin: 1.5rem 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: .75rem 1.5rem; align-items: start; }
  .deck-form fieldset { border: 0; padding: 0; margin: 0; min-width: 0; }
  .deck-form legend { font-weight: 600; font-size: .85rem; padding: 0; margin-bottom: .2rem; }
  .deck-form .row { display: flex; flex-wrap: wrap; gap: .25rem .9rem; align-items: center; }
  .deck-form .row label { display: flex; align-items: center; gap: .3rem; white-space: nowrap; }
  .deck-form input[type="number"] { width: 3.6rem; min-height: 34px; padding: .2rem .4rem; }
  .deck-form button { justify-self: start; min-height: 38px; }
  .deck { font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: .85rem; line-height: 1.5; }
</style>
